import React from 'react'
import AuthenticationButton from './authentication-button'

const AuthNav = ({ messagelogin, messagelogout }) => (
  <AuthenticationButton messagelogin={messagelogin} messagelogout={messagelogout} />
)

export default AuthNav
