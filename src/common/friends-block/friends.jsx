//import contact from './contact.module.css'

function Friends({ couchItems }) {
  return <div className='row'>{couchItems}</div>
}
export default Friends

/**TODO guard for friedns and couches, add component for it */
